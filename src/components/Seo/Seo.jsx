import React from 'react';
import { Helmet } from "react-helmet";

const SEO = ({ title }) => {
  return (
    <Helmet>
      <title>Codeartisan.com.mm</title>
    </Helmet>
  );
};

export default SEO;
